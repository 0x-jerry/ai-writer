# `scene_cards/` 说明

场景卡目录。

建议字段：
- `scene_id`
- `chapter_id`
- `goal`
- `conflict`
- `turn`
- `outcome`

用途：确保场景具备推进功能而非纯填充。

## 操作指引
1. 每个章节至少准备一张场景卡。
2. 每张场景卡必须有明确 `goal` 和 `outcome`。
3. 场景卡中的冲突应可映射到角色/主线推进。
4. 场景完成后更新结果，避免计划与正文脱节。
