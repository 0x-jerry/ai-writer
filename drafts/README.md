# `drafts/` 说明

草稿区（未入典）。

存放待 preflight 的章节草稿和候选设定。
用途：隔离未确认内容，避免污染 canon。

## 操作指引
1. 所有新写内容先进入 `drafts/`，不要直接写 `chapters/`。
2. 草稿需标注对应 `chapter_id`。
3. preflight `BLOCKED` 时只在此目录修复。
4. 通过后由 commit 流程迁移到 `chapters/`。
