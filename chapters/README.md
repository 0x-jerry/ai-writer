# `chapters/` 说明

已入典章节目录（canon chapters）。

仅存放通过 preflight 且 commit 后的正式章节。
用途：作为正式正文真相源。

## 操作指引
1. 仅将 `PASS` 或 `PASS_WITH_RETCON` 的草稿写入此目录。
2. 文件名与 `chapter_id` 保持一致（如 `ch-001.md`）。
3. 入典后必须同步更新 `timeline/` 与 `status/current_status.md`。
4. 若章节被 retcon，保留修订记录并更新 `logs/retcons.jsonl`。
