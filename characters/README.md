# `characters/` 说明

> **入口：请先阅读 `overview.md`（角色索引 + 关系图），再进入具体角色文件。**

角色档案目录，每个角色一个文件。

建议字段：
- 核心动机
- 当前阶段状态
- 不可违背设定
- 最近关键事件

用途：保障角色行为和成长线连续性。

## 操作指引
1. 每个角色固定一个文件，不要重复建同名角色。
2. 角色状态变更后，同步记录“触发章节”和“变更原因”。
3. 任何能力/性格重大变化都要先过 preflight 再入典。
4. 对白风格变化要与 `style/voice.md` 保持一致。
