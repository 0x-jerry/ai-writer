# `canon/` 说明

结构化入典事实目录。

用于存储正文以外的高置信度事实快照（如角色状态快照、规则条目快照）。
用途：为后续检索与一致性检查提供稳定事实源。

## 操作指引
1. 仅收录已通过 preflight 的结构化事实。
2. 每条事实建议标注来源章节与更新时间。
3. 与正文冲突时，以最新批准的 retcon 结果为准。
4. 更新事实后同步记录 `logs/mutations.jsonl`。
